@model AlaricandAssociatesElectronicsWholesale.Models.Supplier

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Supplier</legend>

        @Html.HiddenFor(model => model.SupplierID)

        <div class="editor-label">
            @Html.LabelFor(model => model.supplierName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.supplierName)
            @Html.ValidationMessageFor(model => model.supplierName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.supplierDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.supplierDescription)
            @Html.ValidationMessageFor(model => model.supplierDescription)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.imageURL)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.imageURL)
            @Html.ValidationMessageFor(model => model.imageURL)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.supplierLocation)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.supplierLocation)
            @Html.ValidationMessageFor(model => model.supplierLocation)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
